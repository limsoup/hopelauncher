<style>
			.wrapper { background-color: #EFE; }
			h1 {
				font-size: 45px;
				font-weight: 400;
				text-align: center;
			}
			h3 {
				font-size: 30px;
				font-weight: 400;
				text-align: center;
			}
			p {
				max-width: 400px;
				margin: 0 auto;
			}
			table {
				border: 1px solid #CCC;
				border-radius: 6px;
				background-color: white;
				margin: 0 auto;
				font-weight: 300;
			}
			table td{ margin-left: 10px; margin-right: 10px; padding:10px;}
			table tr { margin-top: 10px; margin-bottom: 10px;}
			p { font-size: 18px; font-weight: 300; }
			a {
				text-decoration: none; 
				color: #0088cc;
			}
</style>
<div class="wrapper">
	<h1> Hopelauncher</h1>
	<p>You've just made a pledge to <%= @project.title.capitalize %>. This message is to thank you and confirm your donation of $<%= @donation.dollar_amount%> to <%= @project.title %></p>
	You can review the details of your donation <%= link_to 'here', project_donation_url(@project, @donation) %>
	<br/>
	<table>
		<tr><td>Organization</td><td><%= @project.creator.name %></td></tr>
		<tr><td>Project </td><td><%= @project.title %></td></tr>
		<tr><td>Cardholder Name</td><td><%= @donation.donator.name %></td></tr>
		<tr><td>Amount</td><td><%= @donation.dollar_amount %></td></tr>
		<tr><td>Card Type</td><td><%= @donation[:type] %></td></tr>
		<tr><td>Last 4 Digits</td><td><%= @donation[:last4] %></td></tr>
	</table>
	<br/>
</div>